---
import City from '../../templates/City.astro';
import { cities } from '../../data/cities';

export function getStaticPaths() {
  return cities.map((city) => ({ params: { city: city.slug }, props: { city } }));
}
const { city } = Astro.props;
---
<City lang="en" city={city} />
